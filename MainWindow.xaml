<Window x:Class="DirectoryAnalyzer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:services="clr-namespace:DirectoryAnalyzer.Services"
        Title="Directory Analyzer"
        Height="720" Width="1100"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="240"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Menu Lateral -->
            <Border Grid.Column="0" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,1,0">
                <StackPanel Margin="16,24">
                    <TextBlock Text="Directory Analyzer"
                               Style="{StaticResource TitleTextStyle}"
                               Margin="0,0,0,4"/>
                    <TextBlock Text="Análise corporativa read-only"
                               Style="{StaticResource SubtitleTextStyle}"
                               Margin="0,0,0,20"/>
                    <ListBox x:Name="NavigationMenu"
                             SelectedIndex="0"
                             SelectionChanged="NavigationMenu_SelectionChanged">
                        <ListBoxItem Content="DNS Analyzer"/>
                        <ListBoxItem Content="GPO Analyzer"/>
                        <ListBoxItem Content="SMB Shares Analyzer"/>
                        <ListBoxItem Content="Scheduled Tasks Analyzer"/>
                        <ListBoxItem Content="Local Profiles Analyzer"/>
                        <ListBoxItem Content="Service Account Analyzer"/>
                        <ListBoxItem Content="Local Security Policy Analyzer"/>
                        <ListBoxItem Content="IIS AppPools Analyzer"/>
                        <ListBoxItem Content="Trusts Analyzer"/>
                        <ListBoxItem Content="ProxyAddresses Analyzer"/>
                    </ListBox>
                </StackPanel>
            </Border>

            <!-- Conteúdo Principal -->
            <Grid Grid.Column="1" Margin="24">
                <ContentControl x:Name="ContentArea"/>
            </Grid>
        </Grid>

        <!-- Status bar -->
        <Border Grid.Row="1" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1,0,0,0">
            <DockPanel Margin="24,10">
                <TextBlock Text="{Binding StatusMessage, Source={x:Static services:StatusService.Instance}}"
                           Style="{StaticResource CaptionTextStyle}"/>
            </DockPanel>
        </Border>
    </Grid>
</Window>
