<UserControl x:Class="DirectoryAnalyzer.Views.DnsAnalyzerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BackgroundBrush}">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
<<<<<<< HEAD
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Style="{StaticResource CardStyle}">
            <DockPanel LastChildFill="False">
                <StackPanel>
                    <TextBlock Text="DNS Analyzer" Style="{StaticResource TitleTextStyle}"/>
                    <TextBlock Text="Coleta zonas, registros e encaminhadores DNS." Style="{StaticResource SubtitleTextStyle}" Margin="0,6,0,0"/>
                </StackPanel>
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Executar Coleta" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding RunCommand}"/>
                    <Button Content="Cancelar" Style="{StaticResource DangerButtonStyle}" Command="{Binding CancelCommand}" Margin="10,0,0,0"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Feedback de execução -->
        <StackPanel Grid.Row="1" Margin="2,10,0,0">
            <ProgressBar IsIndeterminate="True"
                         Style="{StaticResource InlineProgressBarStyle}"
                         Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            <TextBlock x:Name="ProgressText"
                       Text="{Binding ProgressMessage}"
                       Style="{StaticResource CaptionTextStyle}"
                       Foreground="{StaticResource PrimaryBrush}"
                       Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"
                       Margin="0,8,0,0"/>
        </StackPanel>

        <!-- Resultados -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Margin="0,16,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TabControl x:Name="ReportTabs" SelectedIndex="{Binding SelectedTabIndex}">
                    <TabItem Header="Zonas">
                        <DataGrid x:Name="ZonasGrid" AutoGenerateColumns="True" ItemsSource="{Binding Zones}"/>
                    </TabItem>

                    <TabItem Header="Registros">
                        <DataGrid x:Name="RegistrosGrid" AutoGenerateColumns="True" ItemsSource="{Binding Records}"/>
                    </TabItem>

                    <TabItem Header="Encaminhadores">
                        <DataGrid x:Name="ForwardersGrid" AutoGenerateColumns="True" ItemsSource="{Binding Forwarders}"/>
                    </TabItem>
                </TabControl>

                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
                    <Button Content="Exportar CSV" Style="{StaticResource OutlineButtonStyle}" Command="{Binding ExportCsvCommand}" ToolTip="Exporta a aba selecionada em CSV."/>
                    <Button Content="Exportar XML" Style="{StaticResource OutlineButtonStyle}" Command="{Binding ExportXmlCommand}" Margin="10,0,0,0" ToolTip="Exporta a aba selecionada em XML."/>
                    <Button Content="Exportar HTML" Style="{StaticResource OutlineButtonStyle}" Command="{Binding ExportHtmlCommand}" Margin="10,0,0,0" ToolTip="Exporta a aba selecionada em HTML."/>
                    <Button Content="Exportar SQL" Style="{StaticResource OutlineButtonStyle}" Command="{Binding ExportSqlCommand}" Margin="10,0,0,0" ToolTip="Exporta a aba selecionada para SQL."/>
                </StackPanel>

                <TextBlock Grid.Row="2"
                           Text="{Binding StatusMessage}"
                           Style="{StaticResource CaptionTextStyle}"
                           Margin="0,12,0,0"/>
            </Grid>
=======
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Style="{StaticResource CardStyle}">
            <DockPanel LastChildFill="False">
                <StackPanel>
                    <TextBlock Text="DNS Analyzer" Style="{StaticResource TitleTextStyle}"/>
                    <TextBlock Text="Coleta completa de zonas, registros e encaminhadores." Style="{StaticResource SubtitleTextStyle}" Margin="0,6,0,0"/>
                </StackPanel>
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Executar Coleta" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding RunCommand}"/>
                    <Button Content="Cancelar" Style="{StaticResource DangerButtonStyle}" Command="{Binding CancelCommand}" Margin="10,0,0,0"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Mensagem de Progresso -->
        <TextBlock x:Name="ProgressText"
                   Grid.Row="1"
                   Text="{Binding ProgressMessage}"
                   Style="{StaticResource CaptionTextStyle}"
                   Foreground="{StaticResource PrimaryBrush}"
                   Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"
                   Margin="6,12,0,0"/>

        <!-- Abas -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Margin="0,16,0,0">
            <TabControl x:Name="ReportTabs" SelectedIndex="{Binding SelectedTabIndex}">
                <TabItem Header="Zonas">
                    <DataGrid x:Name="ZonasGrid" AutoGenerateColumns="True" ItemsSource="{Binding Zones}"/>
                </TabItem>

                <TabItem Header="Registros">
                    <DataGrid x:Name="RegistrosGrid" AutoGenerateColumns="True" ItemsSource="{Binding Records}"/>
                </TabItem>

                <TabItem Header="Encaminhadores">
                    <DataGrid x:Name="ForwardersGrid" AutoGenerateColumns="True" ItemsSource="{Binding Forwarders}"/>
                </TabItem>
            </TabControl>
        </Border>

        <!-- Botões de Exportação -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Exportar CSV" Style="{StaticResource OutlineButtonStyle}" Command="{Binding ExportCsvCommand}"/>
            <Button Content="Exportar XML" Style="{StaticResource OutlineButtonStyle}" Command="{Binding ExportXmlCommand}" Margin="10,0,0,0"/>
            <Button Content="Exportar HTML" Style="{StaticResource OutlineButtonStyle}" Command="{Binding ExportHtmlCommand}" Margin="10,0,0,0"/>
            <Button Content="Exportar SQL" Style="{StaticResource OutlineButtonStyle}" Command="{Binding ExportSqlCommand}" Margin="10,0,0,0"/>
        </StackPanel>

        <!-- Status -->
        <Border Grid.Row="4" Style="{StaticResource CardStyle}" Margin="0,16,0,0" Padding="12">
            <TextBlock x:Name="StatusText" Text="{Binding StatusMessage}" Style="{StaticResource CaptionTextStyle}"/>
>>>>>>> origin/main
        </Border>
    </Grid>
</UserControl>
