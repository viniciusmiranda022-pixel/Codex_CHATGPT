<UserControl x:Class="DirectoryAnalyzer.Views.DnsAnalyzerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#f7f7f7">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Título -->
        <DockPanel Grid.Row="0" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="🧭" FontSize="20" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBlock Text="Directory Analyzer - DNS"
                           FontSize="20"
                           FontWeight="Bold"
                           Foreground="#111111"
                           VerticalAlignment="Center"/>
            </StackPanel>
        </DockPanel>

        <!-- Botão de Execução -->
        <Button Grid.Row="1"
                Content="▶ Executar Coleta Completa de DNS"
                Click="RunFullDnsCollection"
                Background="#0078D7"
                Foreground="White"
                FontWeight="SemiBold"
                Padding="12,6"
                BorderThickness="0"
                Cursor="Hand"
                HorizontalAlignment="Left"
                Margin="0,0,0,10"/>

        <!-- Mensagem de Progresso -->
        <TextBlock x:Name="ProgressText"
                   Grid.Row="2"
                   Text="⏳ Coletando informações de DNS..."
                   FontStyle="Italic"
                   Foreground="#4a90e2"
                   Visibility="Collapsed"
                   Margin="0,0,0,10"/>

        <!-- Abas -->
        <TabControl Grid.Row="3" x:Name="ReportTabs" Background="White" BorderBrush="#ccc" BorderThickness="1">
            <TabItem Header="Zonas">
                <DataGrid x:Name="ZonasGrid"
                          AutoGenerateColumns="True"
                          AlternatingRowBackground="#f0f0f0"
                          RowBackground="#ffffff"
                          HeadersVisibility="Column"
                          CanUserAddRows="False"
                          Margin="0"
                          FontSize="13"
                          IsReadOnly="True"/>
            </TabItem>

            <TabItem Header="Registros">
                <DataGrid x:Name="RegistrosGrid"
                          AutoGenerateColumns="True"
                          AlternatingRowBackground="#f0f0f0"
                          RowBackground="#ffffff"
                          HeadersVisibility="Column"
                          CanUserAddRows="False"
                          Margin="0"
                          FontSize="13"
                          IsReadOnly="True"/>
            </TabItem>

            <TabItem Header="Encaminhadores">
                <DataGrid x:Name="ForwardersGrid"
                          AutoGenerateColumns="True"
                          AlternatingRowBackground="#f0f0f0"
                          RowBackground="#ffffff"
                          HeadersVisibility="Column"
                          CanUserAddRows="False"
                          Margin="0"
                          FontSize="13"
                          IsReadOnly="True"/>
            </TabItem>
        </TabControl>

        <!-- Botões de Exportação -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="🗎 Exportar CSV" Margin="5,0" Padding="10,6" Click="ExportCsv_Click"
                    Background="#ffffff" BorderBrush="#0078D7" Foreground="#0078D7"
                    BorderThickness="1" FontWeight="SemiBold" Cursor="Hand"/>
            <Button Content="🗐 Exportar XML" Margin="5,0" Padding="10,6" Click="ExportXml_Click"
                    Background="#ffffff" BorderBrush="#0078D7" Foreground="#0078D7"
                    BorderThickness="1" FontWeight="SemiBold" Cursor="Hand"/>
            <Button Content="🌐 Exportar HTML" Margin="5,0" Padding="10,6" Click="ExportHtml_Click"
                    Background="#ffffff" BorderBrush="#0078D7" Foreground="#0078D7"
                    BorderThickness="1" FontWeight="SemiBold" Cursor="Hand"/>
            <Button Content="🛢 Exportar SQL" Margin="5,0" Padding="10,6" Click="ExportSql_Click"
                    Background="#ffffff" BorderBrush="#0078D7" Foreground="#0078D7"
                    BorderThickness="1" FontWeight="SemiBold" Cursor="Hand"/>
        </StackPanel>

        <!-- Status -->
        <Border Grid.Row="5" Background="#e9f1fb" CornerRadius="4" Padding="10" Margin="0,10,0,0">
            <TextBlock x:Name="StatusText"
                       Text="✔️ Pronto para iniciar a coleta."
                       FontSize="13"
                       Foreground="#004d99"
                       VerticalAlignment="Center"/>
        </Border>
    </Grid>
</UserControl>
