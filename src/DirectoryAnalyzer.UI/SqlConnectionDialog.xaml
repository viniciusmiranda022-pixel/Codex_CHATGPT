<Window x:Class="DirectoryAnalyzer.Dialogs.SqlConnectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Conexão com SQL Server" Height="300" Width="400"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Server -->
        <StackPanel Orientation="Horizontal" Margin="0,5">
            <TextBlock Text="Servidor:" Width="100" VerticalAlignment="Center"/>
            <TextBox x:Name="ServerBox" Width="250" />
        </StackPanel>

        <!-- Database -->
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,5">
            <TextBlock Text="Banco de Dados:" Width="100" VerticalAlignment="Center"/>
            <TextBox x:Name="DatabaseBox" Width="250" />
        </StackPanel>

        <!-- Authentication -->
        <StackPanel Orientation="Horizontal" Grid.Row="2" Margin="0,5">
            <TextBlock Text="Autenticação:" Width="100" VerticalAlignment="Center"/>
            <CheckBox x:Name="UseWindowsAuth" Content="Windows Authentication" Checked="UseWindowsAuth_Checked" Unchecked="UseWindowsAuth_Unchecked" IsChecked="True"/>
        </StackPanel>

        <!-- Username -->
        <StackPanel Orientation="Horizontal" Grid.Row="3" Margin="0,5">
            <TextBlock Text="Usuário:" Width="100" VerticalAlignment="Center"/>
            <TextBox x:Name="UsernameBox" Width="250" IsEnabled="False"/>
        </StackPanel>

        <!-- Password -->
        <StackPanel Orientation="Horizontal" Grid.Row="4" Margin="0,5">
            <TextBlock Text="Senha:" Width="100" VerticalAlignment="Center"/>
            <PasswordBox x:Name="PasswordBox" Width="250" IsEnabled="False"/>
        </StackPanel>

        <!-- Botões -->
        <StackPanel Orientation="Horizontal" Grid.Row="5" Margin="0,15,0,0" HorizontalAlignment="Right">
            <Button Content="OK" Width="80" Margin="5" Click="Ok_Click"/>
            <Button Content="Cancelar" Width="80" Margin="5" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
