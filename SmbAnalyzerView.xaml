<UserControl x:Class="DirectoryAnalyzer.Views.SmbAnalyzerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="800"
             Background="{StaticResource BackgroundBrush}">

    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Style="{StaticResource CardStyle}">
            <DockPanel LastChildFill="False">
                <StackPanel>
                    <TextBlock Text="SMB Shares Analyzer" Style="{StaticResource TitleTextStyle}"/>
                    <TextBlock Text="Inventário de compartilhamentos e permissões SMB." Style="{StaticResource SubtitleTextStyle}" Margin="0,6,0,0"/>
                </StackPanel>
                <Button x:Name="ExecuteButton"
                        DockPanel.Dock="Right"
                        Content="Executar Coleta"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Click="RunSmbCollection"/>
            </DockPanel>
        </Border>

        <!-- Filtros -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,16,0,0">
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <StackPanel Margin="0,0,16,0">
                        <TextBlock Text="Atributo de Escopo" Style="{StaticResource CaptionTextStyle}"/>
                        <TextBox x:Name="ScopeAttributeBox" Width="220" Tag="ex: Department"/>
                    </StackPanel>
                    <StackPanel>
                        <TextBlock Text="Valor do Atributo" Style="{StaticResource CaptionTextStyle}"/>
                        <TextBox x:Name="ScopeValueBox" Width="240" Tag="ex: TI"/>
                    </StackPanel>
                </StackPanel>
                <TextBlock Text="Dica: use um atributo do AD para filtrar os servidores alvo." Style="{StaticResource CaptionTextStyle}" Margin="0,10,0,0"/>
            </StackPanel>
        </Border>

        <!-- Feedback de execução -->
        <StackPanel Grid.Row="2" Margin="2,10,0,0">
            <ProgressBar x:Name="ProgressBar"
                         IsIndeterminate="True"
                         Style="{StaticResource InlineProgressBarStyle}"
                         Visibility="Collapsed"/>
            <TextBlock x:Name="ProgressText"
                       Text="⏳ Coletando informações de SMB Shares..."
                       Style="{StaticResource CaptionTextStyle}"
                       Foreground="{StaticResource PrimaryBrush}"
                       Visibility="Collapsed"
                       Margin="0,8,0,0"/>
        </StackPanel>

        <!-- Resultados -->
        <Border Grid.Row="3" Style="{StaticResource CardStyle}" Margin="0,16,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <DataGrid x:Name="SmbGrid" AutoGenerateColumns="True"/>

                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
                    <Button x:Name="ExportCsvButton" Content="Exportar CSV" Style="{StaticResource OutlineButtonStyle}" Click="ExportCsv_Click" ToolTip="Exporta a lista atual em CSV."/>
                    <Button x:Name="ExportXmlButton" Content="Exportar XML" Style="{StaticResource OutlineButtonStyle}" Click="ExportXml_Click" Margin="10,0,0,0" ToolTip="Exporta a lista atual em XML."/>
                    <Button x:Name="ExportHtmlButton" Content="Exportar HTML" Style="{StaticResource OutlineButtonStyle}" Click="ExportHtml_Click" Margin="10,0,0,0" ToolTip="Exporta a lista atual em HTML."/>
                    <Button x:Name="ExportSqlButton" Content="Exportar SQL" Style="{StaticResource OutlineButtonStyle}" Click="ExportSql_Click" Margin="10,0,0,0" ToolTip="Exporta a lista atual para SQL."/>
                </StackPanel>

                <TextBlock Grid.Row="2" x:Name="StatusText" Text="✔️ Pronto para iniciar a coleta." Style="{StaticResource CaptionTextStyle}" Margin="0,12,0,0"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
